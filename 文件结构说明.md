# 📁 项目文件结构说明

## 核心文件

```
web/
├── app/
│   ├── api/
│   │   └── simulate/
│   │       └── route.ts              # 🔥 DeepSeek API调用接口
│   ├── simulator/
│   │   └── page.tsx                  # 🔥 对话式模拟器主界面
│   ├── page.tsx                      # 首页（指向simulator）
│   ├── layout.tsx                    # 布局组件
│   └── Dashboard.tsx                 # 旧版Dashboard（已废弃）
│
├── public/                           # 静态资源
├── .env.local.example                # 🔥 API配置模板
├── .env.local                        # 你的实际配置（不会提交到Git）
├── .gitignore                        # Git忽略文件（保护API Key）
├── vercel.json                       # 🔥 Vercel部署配置
├── package.json                      # 项目依赖
├── tsconfig.json                     # TypeScript配置
└── next.config.ts                    # Next.js配置
```

## 🔥 关键文件说明

### 1. `app/api/simulate/route.ts`
- **作用**：DeepSeek API调用的后端接口
- **功能**：
  - 接收角色、局势、历史数据
  - 构建AI Prompt
  - 调用DeepSeek API
  - 解析结构化决策
  - 返回JSON结果

### 2. `app/simulator/page.tsx`
- **作用**：对话式模拟器主界面
- **功能**：
  - 左侧：8个指标实时监控
  - 中间：对话气泡时间轴
  - 右侧：历史人物卡片
  - 用户角色扮演模式
  - 导出时间轴功能

### 3. `.env.local`
- **作用**：存储API密钥
- **内容**：
  ```env
  DEEPSEEK_API_KEY=sk-你的密钥
  ```
- **注意**：这个文件不会被Git跟踪

### 4. `vercel.json`
- **作用**：Vercel部署配置
- **内容**：
  - 构建命令
  - 环境变量定义
  - 区域设置（新加坡、香港）

## 文档文件

```
web/
├── README.md                         # 🔥 项目概览（2.0版本）
├── QUICKSTART.md                     # 🔥 5分钟快速开始
├── DEPLOY.md                         # 🔥 Vercel部署完整指南
├── PROJECT_SUMMARY.md                # 🔥 项目完整总结
├── 开始使用.md                        # 🔥 中文使用说明
├── 立即开始.md                        # 🔥 3步快速启动
└── 项目交付报告.md                     # 🔥 完整交付报告
```

## 工具脚本

```
web/
├── start.sh                          # 🔥 启动脚本（带API Key检查）
└── test-api.sh                       # 🔥 API测试脚本
```

## 依赖管理

```
web/
├── package.json                      # 依赖列表
├── package-lock.json                 # 锁定版本
└── node_modules/                     # 依赖包（不提交到Git）
```

## 构建产物（不提交到Git）

```
web/
├── .next/                            # Next.js构建缓存
├── .vinext/                          # Vinext开发缓存
├── dist/                             # 生产构建输出
└── .wrangler/                        # Cloudflare Workers配置
```

---

## 🔥 你需要关注的文件

### 开发阶段

1. **`.env.local`** - 配置API Key
2. **`app/simulator/page.tsx`** - 修改界面和逻辑
3. **`app/api/simulate/route.ts`** - 修改AI Prompt和API调用

### 部署阶段

1. **`vercel.json`** - 配置部署参数
2. **`.gitignore`** - 确保敏感信息不泄露

### 使用阶段

1. **`README.md`** - 项目介绍
2. **`QUICKSTART.md`** - 快速上手
3. **`DEPLOY.md`** - 部署指南

---

## 文件大小概览

| 文件类型 | 数量 | 总大小 |
|---------|------|--------|
| TypeScript源码 | 4个 | ~20KB |
| 文档文件 | 7个 | ~100KB |
| 配置文件 | 5个 | ~5KB |
| 工具脚本 | 2个 | ~3KB |

---

## Git跟踪状态

### ✅ 提交到Git
- 所有源码 (`.ts`, `.tsx`)
- 所有文档 (`.md`)
- 配置文件 (`package.json`, `tsconfig.json`, `vercel.json`)
- 工具脚本 (`.sh`)
- API配置模板 (`.env.local.example`)

### ❌ 不提交到Git
- `.env.local` (包含真实API Key)
- `node_modules/` (依赖包)
- `.next/`, `.vinext/`, `dist/` (构建产物)
- `.wrangler/` (临时文件)

---

## 如何查看完整目录结构

```bash
cd web

# 查看所有文件（排除node_modules）
find . -not -path '*/node_modules/*' -not -path '*/.next/*' -type f

# 查看TypeScript文件
find . -name '*.ts' -o -name '*.tsx' | grep -v node_modules

# 查看文档文件
ls -lh *.md

# 查看配置文件
ls -lh *.json
```

---

**提示**：以上标记🔥的文件是核心文件，其他文件都是自动生成或依赖包。
